# Copy this file to ./config/datasource-secrets.yml and hydrate the values from your
# chosen secret store (GitHub Secrets for local runners, AWS Secrets Manager
# for deployed environments). Never commit the real datasource-secrets.yml file.
spring:
  datasource:
    url: ${BILLING_DB_URL:jdbc:mysql://34.100.183.117:3306/billing_consumer}
    driver-class-name: ${BILLING_DB_DRIVER:com.mysql.cj.jdbc.Driver}
    username: ${BILLING_DB_USERNAME:billing_consumer_app}
    password: ${BILLING_DB_PASSWORD:India@JAN@2026}

